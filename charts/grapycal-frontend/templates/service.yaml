apiVersion: v1
kind: Service
metadata:
  name: {{ include "grapycal-frontend.fullname" . }}
  labels:
    {{- include "grapycal-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grapycal-frontend.selectorLabels" . | nindent 4 }}
